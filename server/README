The log storage is set up to be hosted by Apache for later re-downloading

Relevant Apache config:

<Directory /var/www/lol-netlogs/>
Header set Access-Control-Allow-Origin "*"
Options -Indexes
</Directory>
