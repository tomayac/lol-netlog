Server to store logs for Logs of Lag.

The app is mostly client-side, the server is only needed to store
logs for a shareable URL. Right now the server is a crappy Python CGI,
there's also a Node version hidden in a git branch.
